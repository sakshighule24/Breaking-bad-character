import React from 'react';
import image from '../img/footer.png'

const Footer = () => {
  return (
    <footer className='center footerImg'>
      <img src={image} alt='' />
    </footer>
  );
}

export default Footer;