import React from 'react';
import image from '../img/header.png'

const Header = () => {
  return (
    <header className='center'>
      <img src={image} alt='' />
    </header>
  );
}

export default Header;